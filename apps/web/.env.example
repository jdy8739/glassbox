# Backend API URL
# REQUIRED: Backend API endpoint
# Development: http://localhost:4000
# Production: https://api.your-domain.com
NEXT_PUBLIC_API_URL=http://localhost:4000

# NextAuth Configuration
# REQUIRED: NextAuth.js configuration
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# CRITICAL SECURITY: NextAuth secret
# - MUST be at least 32 characters long
# - MUST match backend JWT_SECRET/NEXTAUTH_SECRET
# - Use cryptographically random values
# - Never commit actual secrets to version control
#
# Generate a secure secret:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# This MUST match the backend's JWT_SECRET/NEXTAUTH_SECRET exactly!
NEXTAUTH_SECRET=your-secret-key-here-min-32-chars-CHANGE-THIS

# Google OAuth Credentials
# REQUIRED: Get these from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
#
# Setup instructions:
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 3. Production: Add https://your-domain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
