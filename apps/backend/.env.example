# Server
PORT=4000
NODE_ENV=development

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/glassbox

# JWT Authentication
# IMPORTANT: JWT_SECRET and NEXTAUTH_SECRET must match the frontend's NEXTAUTH_SECRET
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-minimum-32-characters
NEXTAUTH_SECRET=your-secret-key-minimum-32-characters
JWT_EXPIRATION=604800

# Google OAuth (for reference only - OAuth is handled by NextAuth.js in frontend)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Frontend
FRONTEND_URL=http://localhost:3000
